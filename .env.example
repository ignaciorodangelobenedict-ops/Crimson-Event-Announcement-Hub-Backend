# Environment Variables for Production

# Railway automatically sets these - DO NOT SET MANUALLY
# PORT - Automatically set by Railway
# RAILWAY_STATIC_URL - Automatically set by Railway

# Database (Railway provides MySQL/PostgreSQL)
# For Railway MySQL:
DB_HOST=${MYSQLHOST}
DB_USER=${MYSQLUSER}
DB_PASSWORD=${MYSQLPASSWORD}
DB_NAME=${MYSQLDATABASE}
DB_PORT=${MYSQLPORT}

# For Railway PostgreSQL (if using instead):
# DATABASE_URL=${DATABASE_URL}

# JWT Secrets - Generate new ones for production
JWT_SECRET=your_super_secure_jwt_secret_here
JWT_RESET_SECRET=your_separate_reset_secret_here

# Google Auth
GOOGLE_CLIENT_ID=your_google_client_id

# Email service (Brevo SMTP)
SMTP_HOST=smtp-relay.brevo.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=af7c8f001@smtp-brevo.com
SMTP_PASSWORD=xsmtpsib-fd5322ccbece832bdac61bf7e146c22a69329f3628948fbdefe20fe844a7d99c-NH7Z2TkOsieQMiPL
SENDER_EMAIL=ignaciorodangelobenedict@gmail.com

# Frontend URL (Your Vercel deployment URL)
FRONTEND_URL=https://your-vercel-app.vercel.app

# Server (Railway sets PORT automatically)
# PORT=5100